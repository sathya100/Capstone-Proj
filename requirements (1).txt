# SafeRx — Python dependencies
# Pin exact versions once confirmed during Wk4 environment setup; ranges below are safe starting points.

# Core / data handling
numpy>=1.26,<2.0
pandas>=2.1,<3.0
scikit-learn>=1.4,<2.0

# ML classifier (severity / interaction prediction)
xgboost>=2.0,<3.0
lightgbm>=4.3,<5.0

# Retrieval (embedding index for RAG)
faiss-cpu>=1.8,<2.0
sentence-transformers>=2.6,<3.0

# LLM API access (explanation generation + verification)
anthropic>=0.34,<1.0

# Verification layer (NLI / semantic-similarity claim checking)
transformers>=4.40,<5.0
torch>=2.2,<3.0

# Backend API
flask>=3.0,<4.0
flask-cors>=4.0,<5.0

# Frontend (only if using Streamlit instead of React)
streamlit>=1.33,<2.0

# Dev / eval
jupyter>=1.0,<2.0
matplotlib>=3.8,<4.0
pytest>=8.0,<9.0
python-dotenv>=1.0,<2.0
